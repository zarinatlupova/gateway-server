spring:
  application:
    name: gateway-service

  cloud:
    config:
      uri: http://localhost:8888  # URL config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka